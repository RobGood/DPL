{
   "Info" : {
      "ChangeLog" : "This is a record of DPL changes by version, and a service to report the changes.",
      "Credits" : "Thanks to contributors.",
      "DPL-demo-template" : "This is a template to copy as the start of a DPL demo app",
      "HTML-macros" : "This provides some basic HTML generating functions.",
      "README" : "DPL (Descriptor/PrimaLisp) Components",
      "README.info" : "This updates the README.info cache.",
      "README.info.null" : "This updates the README.info cache.",
      "desc-function-index" : "This generates forward and reverse indexes between function names and where they are defined.",
      "dpl-getting-started" : "This is an introduction to using DPL.",
      "dple" : "A placeholder for DPL implementation of DPL Editor.",
      "dplx" : "This is the entry point for the DPL dispatcher.",
      "dplx-docs" : "DPL webservice request dispatcher docs",
      "echo" : "Reply with the incoming request details.",
      "eval" : "This implements the DPL eval webservice endpoint",
      "eval-docs-examples" : "This is the source for the eval examples.",
      "get-DPL-dist" : "Call this to download a DPL distribution file.",
      "http-peer-server" : "A standalone HTTP server for dplx",
      "hw" : "'Hello world' in DPL, slightly embellished.",
      "incl" : "This endpoint returns various gps variables stored under 'incl'",
      "man" : "Reference manual pages",
      "pretty-print" : "This pretty prints a descriptor and shows differences between two revisions.",
      "user" : "This mucks with something to affect authenticated user state.",
      "wk-log" : "This summarizes edit stats on descriptors under version control."
   },
   "README" : "This updates the README.info cache.",
   "_dateCreated" : "Tue Jun  7 15:51:06 2011",
   "_useCount" : 31,
   "info" : "(fn () (hash-get $self / Info))",
   "main" : "(fn () (reply-json (update)))",
   "on-load" : "(fn ()\n  (def-hash-fn $self {\n    / [\n      info\n      update\n    ]\n  })\n)",
   "rcs-id" : "$Id: README.info,v 1.7 2013/04/18 19:20:40 apache Exp apache $",
   "update" : "(def-fn update-one (d-name)\n  (desc-read d $d-name {no-cache 1 no-on-load 1})\n\n  (if (undef? (= rm (hash-get $d / README)))\n    (= rm (hash-get $d / 00)))\n      \n  (if (string? $rm)\n    (hash-set $out / $d-name (first (split '\\n' $rm)))))\n\n(fn ()\n  (= out  {})\n  (= name $self/name)\n\n  (foreach d-name (desc-list *)\n    (report 'Updating %s for %s...' $name $d-name)\n\n    (catch\n      (update-one $d-name)\n      (report-vars _err _cs)))\n\n  (hash-set $self / Info $out)\n\n  (desc-write $self $name)\n\n  $out\n)"
}
